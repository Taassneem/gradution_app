import 'package:flutter/material.dart';

import 'widget/number_quiz_nine_view_body.dart';

class NumberQuizNineView extends StatelessWidget {
  const NumberQuizNineView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: NumberQuizNineViewBody(),
    );
  }
}
