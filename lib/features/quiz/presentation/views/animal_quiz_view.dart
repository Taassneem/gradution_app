import 'package:flutter/material.dart';

class AnimalQuizView extends StatelessWidget {
  const AnimalQuizView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}